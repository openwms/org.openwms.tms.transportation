language: java

jdk:
  - openjdk8

services:
- docker

after_success:
- docker build -t openwms/org.openwms.tms.transportation:latest .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master